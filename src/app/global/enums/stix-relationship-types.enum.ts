export const enum StixRelationshipTypes {
    ATTRIBUTED_TO = 'attributed-to',
    IMPERSONATES = 'impersonates',
    INDICATES = 'indicates',
    MITIGATES = 'mitigates',
    TARGETS = 'targets',
    USES = 'uses',
    VARIANT_OF = 'varient-of',
    X_UNFETTER_CAN_RUN = 'can-run',
    X_UNFETTER_DETECTS = 'detects'
}
