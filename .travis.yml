# Use Ubuntu 'trusty' distribution
sudo: required
dist: trusty
addons:
  chrome: stable
before_install:
  - npm run server:dev:alt
  - google-chrome-stable --headless --disable-gpu http://localhost &
  - npm test