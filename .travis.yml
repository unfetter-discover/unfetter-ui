# Use Ubuntu 'trusty' distribution
dist: trusty
lanugage: node_js
node_js:
  - "io.js"
  - "9"

branches:
  only:
    - develop

addons:
  chrome: stable
before_install:
  - google-chrome-stable --headless --disable-gpu http://localhost &
